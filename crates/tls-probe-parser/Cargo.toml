[package]
name = "tls-probe-parser"
version = "0.1.0"
edition = "2021"
description = "TLS ClientHello/ServerHello parser for tls-probe"

[dependencies]
thiserror = "2.0"

[dev-dependencies]
hex = "0.4"
